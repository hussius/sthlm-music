{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "caddy run --config /assets/Caddyfile --adapter caddyfile",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60
  }
}
