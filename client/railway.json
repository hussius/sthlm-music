{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "/bin/bash -l -c 'caddy run --config /app/Caddyfile'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60
  }
}
