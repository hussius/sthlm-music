# Backend Environment Configuration
# Copy this file to .env and customize for your environment

# Server Configuration
HOST=127.0.0.1
PORT=8098

# Environment Mode
# Options: development, production, test
ENVIRONMENT=development

# Database Configuration (optional - for when you add a database)
# DATABASE_URL=postgresql://user:password@localhost:5432/agents_hackathon
# DATABASE_URL=sqlite:///./agents_hackathon.db

# API Keys (add as needed for your hackathon project)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# CORS Origins (comma-separated list)
# Allows your frontend to make requests to the backend
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Security (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# SECRET_KEY=your-secret-key-here

# Logging
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
